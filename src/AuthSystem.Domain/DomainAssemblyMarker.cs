namespace AuthSystem.Domain;

/// <summary>
/// Assembly marker for DI scanning and test setup.
/// Use <see cref="typeof(DomainAssemblyMarker).Assembly"/> to reference the domain assembly.
/// </summary>
public static class DomainAssemblyMarker
{
}
